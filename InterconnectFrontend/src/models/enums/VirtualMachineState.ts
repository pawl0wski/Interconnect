/**
 * Enumeration of virtual machine runtime states.
 */
enum VirtualMachineState {
    /** Virtual machine state is unknown */
    Unknown = 0,
    /** Virtual machine is booted and running */
    Booted = 1,
}

export { VirtualMachineState };
