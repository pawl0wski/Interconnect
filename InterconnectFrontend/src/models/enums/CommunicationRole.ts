/**
 * Enumeration of communication roles in network packet transfers.
 */
enum CommunicationRole {
    /** Entity is acting as a sender/source */
    Sender = 0,
    /** Entity is acting as a recipient/destination */
    Recipient = 1,
}

export default CommunicationRole;
